# serial-bridge

WindowsのCOMポートをTCPで中継し、WSL側アプリがシリアルのように扱うためのブリッジ。

- 配布物は GitHub Actions の Artifacts から取得
- `serial-bridge.yml` を exe と同じフォルダに置いて起動

注意: 未署名exeのため SmartScreen が出る場合があります（More info -> Run anyway）。


